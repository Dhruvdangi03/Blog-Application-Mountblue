spring:
  application:
    name: blog-application
  datasource:
    url: jdbc:postgresql://localhost:5432/blog
    username: bloguser
    password: password
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
  thymeleaf:
    cache: false
  mvc:
    hiddenmethod:
      filter:
        enabled: true